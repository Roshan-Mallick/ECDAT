# ECDAT
ECDAT is a security tool that finds cryptography inside a software project. It checks source code, certificates, and TLS settings. It then tells the developer what was found, whether it is safe, how risky it is, and whether it may need Post-Quantum Cryptography (PQC) migration. ECDAT will be built as a standalone product. It will not depend on
